# Archive index

History. Nothing here is read routinely: `.rgignore` keeps `archive/` out of default searches, so a
plain search will not see it. Read a file by path when a live note points here, or search it explicitly
(`rg --no-ignore`, or a search scoped to `archive/`).

**Never restate archive content into a live file** — link to it. The point of the archive is that the
live files stay small enough to read in full at the start of a session.

## What goes here

- session log entries, once `RESEARCH_LOG.md` passes a few hundred lines;
- plans that have been executed or abandoned, and closed to-do files;
- a README or a note as it stood before being cut down, if the cut lost something worth recovering;
- long-form investigations and measurements that a live note only needs to cite.

Superseded **writing** goes to `writing/_archive/` instead, with a `DESCRIPTION.md` — that is where the
theory's history lives, and it is reserved as the basis for a later, more purely theoretical paper.

Archive by `git mv`, so the history follows the file, and leave a pointer in whatever file lost the
content.

## Contents

| | |
|---|---|
| `README_theoryDigest_2026-09-17.md` | `README.md` as it stood before being cut to a repository map. Its digest of the settled theory results — Little's law for matter, the state-C price block, the closure criterion, the BDP rate matching, the shutdown, and where the non-convexity lives — is a useful orientation, but `writing/docs/` owns every claim in it |
